import React from 'react';

import Server from './server';

const ServerList = () => (
  <div>
    Server List

    <Server server={{}}/>
    <Server server={{}}/>
    <Server server={{}}/>
    <Server server={{}}/>
    <Server server={{}}/>
    <Server server={{}}/>
  </div>
);

export default ServerList;
