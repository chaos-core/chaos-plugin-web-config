import React from 'react';

const ChangeServerBtnView = ({onClick}) => (
  <div>
    <div className={"btn btn-small"} onClick={onClick}>Change Server</div>
  </div>
);

export default ChangeServerBtnView;
